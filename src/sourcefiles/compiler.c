#include "bytecode.h"
#include "compiler.h"
#include "expression.h"

Instruction_t* compile_expression(Expresion_t* expr) {
    switch (expr->type) {
        case ExpressionType_Literal:
    }
}

Instruction_t* compile(Expression_t* expr) {

}
